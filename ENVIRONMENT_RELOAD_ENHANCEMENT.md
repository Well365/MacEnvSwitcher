# 环境切换增强功能

## 新增功能概览

### 🔄 **系统配置重新载入**
当您选择一个环境组并进行切换激活后，系统现在会自动：

1. **重新载入Shell配置** - 自动执行 `source ~/.zshrc` 应用最新配置
2. **更新系统默认版本** - 设置 asdf 全局版本为当前环境的配置
3. **刷新命令shims** - 确保所有工具的路径正确更新
4. **应用环境变量** - 将环境变量应用到当前进程和系统
5. **验证配置生效** - 检查所有设置是否正确应用

### 💾 **系统级配置持久化**

#### UserDefaults 存储
- 活跃配置文件名称
- 最后切换时间
- 各语言版本配置
- 环境变量设置

#### 配置文件生成
自动在 `~/.macenvswitcher/` 目录创建：
```bash
current_environment.sh  # 当前环境配置脚本
```

这个脚本包含：
- asdf 全局版本设置命令
- 环境变量导出语句
- 自动执行的配置命令

### 🚀 **启动时配置恢复**
应用启动时会自动：
1. 检查是否存在之前保存的配置
2. 恢复上次激活的环境设置
3. 重新应用所有配置到系统

### 🔍 **增强的环境切换验证**

#### 实时验证
切换环境后自动检查：
- ✅ asdf 版本是否正确设置
- ✅ 环境变量是否正确应用
- ✅ 命令shims是否更新
- ✅ 终端配置是否同步

#### 详细日志
提供完整的切换日志，包括：
- 配置文件更新状态
- 版本设置结果
- 环境变量应用情况
- 错误和警告信息

### 🎯 **终端集成**

#### iTerm2 支持
- 自动更新终端窗口标题
- 在终端中显示当前环境名称
- 支持终端配置文件同步

#### Shell 集成
- 自动更新 `.zshrc` 配置
- 支持多种Shell环境
- 智能配置合并

## 使用方式

### 1. 选择环境组
在主界面的"环境配置管理"区域：
1. 从下拉菜单选择您想要的环境组
2. 点击"切换激活"按钮

### 2. 自动应用配置
系统会自动：
- 停用当前环境
- 应用新环境配置
- 重新载入系统设置
- 验证配置是否生效

### 3. 查看切换结果
在asdf区域的日志中可以看到：
- 详细的切换过程
- 各项配置的应用状态
- 任何错误或警告信息

## 配置文件位置

### 应用配置
```
~/Library/Preferences/com.example.MacEnvSwitcher.plist
```

### 系统配置
```
~/.macenvswitcher/
├── current_environment.sh     # 当前环境配置脚本
└── backup/                    # 配置备份（未来功能）
```

### Shell 配置
```
~/.zshrc                       # 自动更新的Shell配置
```

## 影响范围

### ✅ **系统级别**
- 全局 asdf 版本设置
- 系统环境变量
- Shell 配置文件
- 终端配置

### ✅ **应用级别**
- 界面状态同步
- 版本选择器更新
- 配置文件管理
- 日志和状态显示

### ✅ **会话级别**
- 当前进程环境变量
- 终端窗口标题
- 命令路径更新

## 技术实现

### 核心改进
1. **`Installers.reloadSystemConfiguration()`** - 系统配置重载
2. **`ProfilesStore.applySystemConfiguration()`** - 系统级配置应用
3. **`TaskModel.reloadVersionManager()`** - 版本管理器刷新
4. **`MacEnvSwitcherApp.restoreSystemEnvironment()`** - 启动时恢复

### 错误处理
- 配置文件读写错误处理
- Shell 命令执行失败处理
- 权限问题检测和提示
- 回滚机制（未来功能）

## 注意事项

1. **首次使用** - 第一次切换环境时可能需要重启终端
2. **权限要求** - 需要写入用户目录的权限
3. **Shell 兼容** - 主要支持 zsh，bash 支持有限
4. **备份建议** - 建议在使用前备份 `.zshrc` 文件

现在您的环境切换不仅会更新应用内的状态，还会真正影响到系统的默认配置！