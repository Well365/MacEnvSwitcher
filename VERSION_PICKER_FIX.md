# MacEnvSwitcher 版本选择器修复

## 问题描述
在可视化编辑的版本选择中，当用户点击"刷新版本"按钮时，会出现asdf命令的帮助信息和描述文本，而不是相关的软件版本号。

## 问题原因
1. **VersionManager.swift**: `executeVersionCommand` 方法直接返回asdf命令的原始输出，未过滤掉帮助信息
2. **Installers.swift**: `listVersions` 方法同样返回原始命令输出，包含大量无关信息

## 修复内容

### 1. VersionManager.swift 修复
- 添加了智能过滤逻辑，过滤掉命令帮助信息
- 只保留实际的版本号格式数据
- 支持识别多种版本格式（数字版本、Java版本、特殊标记等）

```swift
// 过滤示例：
// 输入：包含 "asdf exec <command>" 等帮助信息
// 输出：只保留 "3.11.6", "3.12.0" 等版本号
```

### 2. Installers.swift 修复
- 重新设计 `listVersions` 方法的输出格式
- 使用清晰的中文界面显示版本信息
- 分别显示：已安装版本、当前使用版本、最新可用版本

```swift
// 新的输出格式：
📦 Python 版本信息

🟢 已安装版本:
   • 3.11.6
   • 3.12.0

🎯 当前使用版本:
   ✓ 3.12.0

📋 最新可用版本 (最近20个):
   • 3.8.18
   • 3.9.18
   • 3.10.13
   • 3.11.6
   • 3.12.0
```

### 3. 代码优化
- 修复了未使用变量的警告
- 改进了错误处理逻辑
- 统一了代码风格

## 过滤规则
新的版本过滤逻辑会排除以下内容：
- 命令帮助信息（如 "Executes the command", "Runs util" 等）
- 资源链接（如 "GitHub:", "Docs:" 等）  
- 插件信息（如 "PLUGIN nodejs" 等）
- 名言引用（如 "Late but latest" 等）
- 过长的描述行（超过50字符）
- 命令参数格式（如 "[args...]", "<command>" 等）

## 支持的版本格式
- 标准数字版本：3.11.6, 1.21.4
- Java版本：openjdk-21, temurin-21
- 特殊标记：latest, system, stable, beta, nightly

## 测试验证
修复后的版本选择器现在能够：
1. 正确显示纯净的版本号列表
2. 提供友好的中文界面
3. 清晰区分已安装、当前使用和可用版本
4. 避免显示无关的命令帮助信息

## 构建状态
✅ 编译成功（有少量非关键警告）
✅ 功能验证通过
✅ 用户界面改进完成